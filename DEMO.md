# 발표용 시연 좌표

C가 T+6~12 사이에 검증하기로 한 "전북 밖이 전주보다 빠른" 좌표. 실제 심평원 데이터(B의
`/api/sync`로 수집한 1,894건)와 실제 Tmap 교통시간으로 검증 완료 — 직선거리 격자 스캔으로
후보를 추린 뒤, 상위 5곳을 실제 Tmap API 호출로 재확인했다.

## 메인 데모 좌표 ★

```
lat=36.1, lng=127.3   (완주군 동북부, 논산 경계 부근)
```

- 전북 밖: **성심의원 (충남 논산시)** — 실제 14분 (9.0km)
- 전북 안: 한솔의원 (완주군) — 실제 27분 (21.8km)
- **약 2배 차이.** CLAUDE.md가 프로젝트 설명에서 직접 예로 든 "전주보다 논산이 빠르다"는
  서사와 그대로 맞아떨어진다.

## 백업 좌표

```
lat=35.3, lng=127.3   (남원시 동부, 전남 곡성 경계 부근)
```

- 전북 밖: **정석진의원 (전남 곡성군)** — 실제 6분 (2.5km)
- 전북 안: 노암연합의원 (남원시) — 실제 20분 (16.1km)
- **약 3.3배 차이.** 격차가 가장 극적이라 메인이 네트워크 문제로 막히면 이걸로 대체.

## 검증 방법 (재현하려면)

1. 로컬 서버에서 `/api/sync`를 호출해 심평원 원본 데이터를 받는다 (Supabase 저장 없이도 fetch
   자체는 동작함, ~1분 소요).
2. 전북을 감싸는 사각형(위도 35.30~36.10, 경도 126.55~127.90)을 0.05도(~5km) 격자로 스캔하며
   각 지점에서 반경 60km 내 가까운 8개 후보를 haversine으로 뽑는다.
3. 1위 후보가 전북 밖이고, 전북 내 최선 후보와 직선거리 격차가 3km 이상인 지점만 남긴다.
4. 상위 후보들을 실제 Tmap 경로안내 API로 재확인해 직선거리가 아니라 진짜 교통시간 기준으로도
   성립하는지 검증한다.

## 참고 — B의 `/api/sync`가 아직 Supabase에 저장을 안 해서

이 좌표들은 임시로 심평원 원본 데이터를 직접 스캔해서 찾은 것이다. B가 `/api/sync`에
Supabase upsert를 추가하고 나면, `clinics` 테이블에 실데이터가 들어오고 `/api/nearby`가
이 좌표들로 그대로 실제 서비스 흐름을 재현할 것이다. 발표 10분 전 캐시 워밍(CLAUDE.md §7)도
이 두 좌표로 하면 된다.
